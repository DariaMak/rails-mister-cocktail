<!-- <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%# image_url @garden.banner_url %>);">
  <div class="container"> -->
    <h1><%= @cocktail.name %></h1>
  <!-- </div> -->
</div>

<div class="container mt-5">
  <div class="row">
    <% @cocktail.doses.each do |dose| %>
    <div class="col-xs-12 col-sm-6 col-md-4">
       <%= dose.ingredient.name %>
       <%= dose.description %>
       <%= link_to 'Destroy', dose_path(dose), method: :delete, data: { confirm: 'Are you sure?' } %>

   <%end%>

 </div>
</div>

<div class="container my-5">
 <div class="row">
   <div class="col-xs-12 col-sm-6 offset-sm-3 col-md-4 offset-md-4">
    <h3>Create a new dose!</h3>
    <%= simple_form_for [ @cocktail, @dose ] do |f| %>
    <%= f.input :description, placeholder: "" %>
    <%= f.association :ingredient %>
    <%= f.submit class: "btn btn-outline-success btn-block" %>
    <% end %>
  </div>
 </div>
</div>

<%= link_to 'Cocktails', cocktails_path %>


