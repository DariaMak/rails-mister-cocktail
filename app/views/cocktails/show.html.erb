<!-- <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%# image_url @garden.banner_url %>);">
  <div class="container"> -->
    <h1><%= @cocktail.name %></h1>
  <!-- </div> -->
</div>

<div class="container mt-5">
  <div class="row">
    <% @cocktail.ingredients.each do |ingredient| %>
    <div class="col-xs-12 col-sm-6 col-md-4">
      <!-- <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= image_url plant.image_url %>)"> -->
       <%= ingredient.name %>
       <%= dose.description %>
     <!-- </div> -->
        <%#= link_to "+", plant_path(plant), method: :delete, data: { confirm: "Kill this plant?" }, class: "card-remove-link"  %>
   <!-- </div> -->
   <%# end %>
   <%end%>

 </div>
</div>

<div class="container my-5">
 <div class="row">
   <div class="col-xs-12 col-sm-6 offset-sm-3 col-md-4 offset-md-4">
    <h3>Create a new dose!</h3>
    <%= simple_form_for [ @cocktail, @dose ] do |f| %>
    <%= f.input :description, placeholder: "" %>
    <%= f.association :ingredient %>
    <%= f.submit class: "btn btn-outline-success btn-block" %>
    <% end %>
  </div>
</div>
</div>
