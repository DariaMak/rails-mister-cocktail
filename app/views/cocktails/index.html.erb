<!-- <p id="notice"><%= notice %></p> -->

<h1>Cocktails</h1>
<!--
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Banner url</th>
      <th colspan="3"></th>
    </tr>
  </thead>
 -->
  <body>
    <% @cocktails.each do |cocktail| %>
      <ul>
        <il><%= cocktail.name %></il>
        <!-- <td><%#= cocktail.banner_url %></td> -->
        <il><%= link_to 'Show', cocktail_path(cocktail) %></il>
        <il><%= link_to 'Destroy', cocktail_path(cocktail), method: :delete, data: { confirm: 'Are you sure?' } %></il>
      </ul>
    <% end %>
    <td><%= link_to 'New', new_cocktail_path %></td>
  </body>



