<!-- <p id="notice"><%= notice %></p> -->

<h1>Cocktails</h1>
<!--
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Banner url</th>
      <th colspan="3"></th>
    </tr>
  </thead>
 -->
  <tbody>
    <% @cocktails.each do |cocktail| %>
      <tr>
        <td><%= cocktail.name %></td>
        <!-- <td><%#= cocktail.banner_url %></td> -->
        <td><%= link_to 'Show', cocktail_path(cocktail) %></td>
        <td><%= link_to 'New', new_cocktail_path(cocktail) %></td>
        <td><%= link_to 'Destroy', cocktail_path(cocktail), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>


